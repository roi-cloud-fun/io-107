---
# DELIBERATELY BROKEN — Lab 3 students remediate this file.
#
# Policy: policies/eks.rego
#
# VIOLATION 6: Deployment metadata is missing required labels
#              `environment` and `owner`.
# VIOLATION 7: Container `myapp` pulls from an unapproved registry
#              (`docker.io`). Only images from an Amazon ECR registry are
#              permitted. The approved pattern (per policies/eks.rego):
#                <account-id>.dkr.ecr.<region>.amazonaws.com/<repo>:<tag>
#              OR `public.ecr.aws/<path>:<tag>` (see eks.rego for both).
# VIOLATION 8: Container `myapp` is missing `resources.limits` — both
#              `memory` and `cpu` limits are required.
#
# NOTE: metadata.namespace is intentionally omitted. The pipeline's Deploy
# phase sets the namespace at apply time via `kubectl apply -n lab3`,
# matching the namespace it creates with `kubectl create namespace lab3`.
# Hardcoding `namespace:` here would conflict with that flag.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp
  labels:
    app: myapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: myapp
  template:
    metadata:
      labels:
        app: myapp
    spec:
      containers:
        - name: myapp
          image: docker.io/library/nginx:latest
          ports:
            - containerPort: 80
