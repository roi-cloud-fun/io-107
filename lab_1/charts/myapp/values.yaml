replicaCount: 2

image:
  # Repository is per-student (each student has their own ECR registry).
  # The pipeline injects this via:
  #   --set image.repository=$ECR_REGISTRY/$APP_NAME
  # Defaulting to "" so a missing override fails loudly at `helm template`
  # time rather than silently pulling from someone else's account.
  repository: ""
  # Policy: image tag MUST be set by the pipeline (--set image.tag=$IMAGE_TAG).
  # Default left empty so a missing override fails fast at `helm template` time
  # rather than silently deploying a mutable "latest" tag.
  tag: ""
  pullPolicy: IfNotPresent

service:
  type: LoadBalancer
  port: 80
  targetPort: 8080

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 100m
    memory: 128Mi

serviceAccount:
  create: true
  name: myapp-sa
  annotations:
    eks.amazonaws.com/role-arn: ""

# Container listens on 8080 (see Dockerfile CMD). Service targets 8080.
containerPort: 8080

livenessProbe:
  httpGet:
    path: /health
    port: 8080
  initialDelaySeconds: 10
  periodSeconds: 10
  timeoutSeconds: 2
  failureThreshold: 3

readinessProbe:
  httpGet:
    path: /health
    port: 8080
  initialDelaySeconds: 5
  periodSeconds: 5
  timeoutSeconds: 2
  failureThreshold: 3

# Overridden per-env (values-dev.yaml, values-stg.yaml).
environment: base
